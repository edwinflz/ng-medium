import { UiChessQuoteNextDirective } from './ui-chess-quote-next.directive';

describe('UiChessQuoteNextDirective', () => {
  it('should create an instance', () => {
    const directive = new UiChessQuoteNextDirective();
    expect(directive).toBeTruthy();
  });
});
